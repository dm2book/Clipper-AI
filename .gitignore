__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Prisma tooling. The schema and migrations are committed; the CLI and the
# engine binaries it downloads are not.
db/node_modules/
db/generated/

# Dashboard build output and dependencies.
web/node_modules/
web/dist/
# Regenerated from the running API by `npm run generate:api`; committed types
# live at web/src/api/types.ts instead.
openapi.json
web/tsconfig.tsbuildinfo

# setuptools build output, e.g. from a local `pip install .`
build/
dist/
